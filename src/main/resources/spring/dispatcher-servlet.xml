<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                           http://www.springframework.org/schema/beans/spring-beans.xsd
                           http://www.springframework.org/schema/mvc
                           http://www.springframework.org/schema/mvc/spring-mvc.xsd
                           http://www.springframework.org/schema/context
                           http://www.springframework.org/schema/context/spring-context.xsd">

    <context:annotation-config/>

    <!-- scan the package and the sub package -->
    <context:component-scan base-package="com.hyschool.*.controller"/>

    <!-- if you use annotation you must configure following setting -->
    <mvc:annotation-driven/>

    <!-- don't handle the static resource -->
    <mvc:default-servlet-handler />

    <!-- deal the static resources， css， js， imgs -->
    <mvc:resources mapping="/css/**" location="/static/css/"/>
    <mvc:resources mapping="/images/**" location="/static/images/"/>
    <mvc:resources mapping="/fonts/**" location="/static/fonts/"/>
    <mvc:resources mapping="/js/**" location="/static/js/"/>

    <!-- configure the InternalResourceViewResolver -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver"
          id="internalResourceViewResolver">
        <!-- 前缀 -->
        <property name="prefix" value="/WEB-INF/views/" />
        <!-- 后缀 -->
        <property name="suffix" value=".jsp" />
    </bean>


    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/order/*"/>
            <mvc:mapping path="/business/*"/>
            <mvc:mapping path="/admin/*"/>
            <bean class="com.hyschool.interceptors.LoginInterceptor"/>
        </mvc:interceptor>

        <mvc:interceptor>
            <mvc:mapping path="/business/*"/>
            <bean class="com.hyschool.interceptors.BusinessInterceptor"/>
        </mvc:interceptor>

        <mvc:interceptor>
            <mvc:mapping path="/admin/*"/>
            <bean class="com.hyschool.interceptors.AdminINterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>


</beans>